# 真实验收

按用户要求，已删除全部模拟测试和 pytest 依赖。只使用真实登录态、真实目标账号及真实返回值验证业务流程；既往模拟测试数量不再作为验收依据。

## 首次结果

2026-09-14，使用用户提供的目标文章短链接执行 `scripts/verify_live.py`：

| 环节 | 结果 | 实际证据 |
| --- | --- | --- |
| 登录态验证 | passed | 独立进程使用已保存 Cookie 成功读取书架 |
| 识别目标并订阅 | failed | 文章返回 HTTP 302，跳转 `/mp/wappoc_appmsgcaptcha`；报告 article_requires_verification |
| 发现文章和获取正文 | not_run | 目标公众号未能识别及添加 |
| 读取保存报告 | not_run | 尚无目标采集 run |

整体为 **not_passed**。登录成功不表示采集可用，预览成功不表示订阅成功。未使用其他公众号替换用户目标来产生通过结果。

## 修正后的真实结果

2026-09-15，使用相同用户文章链接重跑：

| 环节 | 结果 | 实际证据 |
| --- | --- | --- |
| 登录态验证 | passed | 保存后的 Cookie 可用 |
| 识别目标并订阅 | passed | 普通浏览器取得 biz，确认公众号名称；真实 POST 添加后书架回读确认，本地监控启用 |
| 发现文章和获取正文 | partial | 列表 -2041，独立验证书架可用后降级 cover；取得最新一篇及 4115 字正文，成功入库 |
| 读取保存报告 | passed | 独立进程重读与当次结果一致 |

记录文件名：`b59fec8bbdac44298c7e845c70c32988.json`。完整验收仍为 not_passed，因为仅能取得最新一篇，无法证明历史和多篇群发覆盖。新增浏览器解析及错误分类修复均以此次真实返回为依据，没有恢复模拟测试。

真实运行的完整 JSON 记录在本机用户数据目录 `mpwatch/data/verification/` 下；真实账号、文章链接、内容和 Cookie 不进入 Git。

## 执行方式

```powershell
uv run python scripts/verify_live.py --article-url '实际文章链接'
# 或
uv run python scripts/verify_live.py --book-id MP_WXS_实际数字ID
```

脚本会真实添加所选公众号到微信读书书架并启用本地监控，然后真实执行 refresh、读取固定报告。任何前置环节失败都会停止依赖步骤，并保存失败记录；返回码 1，不标记通过。

通过条件：认证可用，远端书架回读确认目标订阅，本地清单保存成功，当轮采集包含目标账号的可用正文，且独立进程重读报告与当次结果一致。若无当轮目标正文则为 inconclusive，整体仍 not_passed；若扫描达到页数预算等限制则保留 partial，不伪装完整采集。

当轮已有正文但覆盖不足时仍执行报告持久化核对，collection 保持 partial，整体不升级为 passed。上述证据不证明历史文章完整、多日稳定、发现延迟或正文内容准确性。正文真实性及完整性还需要与手机可见原文核对，尚未完成的验证逐项保持未验证。
